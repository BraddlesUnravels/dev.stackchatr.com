import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="h-full w-full flex items-center justify-center px-6 py-16">
      <h1 class="text-4xl font-bold text-white">Projects</h1>
    </div>
  );
});
